const { createNewBlogController } = require('./createNewBlog');
const { getBlogPostsMetadataController } = require('./getBlogPostsMetadata');

module.exports = {
    createNewBlogController,
    getBlogPostsMetadataController
}